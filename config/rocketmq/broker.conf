# Windows local-development broker advertisement for a host-run app plus Dockerized dashboard.
# RocketMQ is more reliable here with a literal host IPv4 than with `host.docker.internal`.
# Update this value if your Windows host IP changes.
brokerIP1=127.0.0.1
